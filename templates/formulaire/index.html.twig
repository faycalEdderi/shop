{% extends 'base.html.twig' %}

{% block title %}SHOP.FR | ADMINISTRATEUR{% endblock %}

{% block body %}

<div class="col-lg-9">
<h3 class="my-4">AJOUTER UN ARTICLE</h3>
        {{ form_start(form) }}

            {{ form_label(form.name, "nom : " | capitalize) }}
            {{ form_widget(form.name)}}

            {{ form_row(form.description)}}
            {{ form_row(form.image) }}
            {{ form_row(form.price) }}
            {{ form_row(form.category) }}

            <button class="btn btn-primary">Valider</button>

        {{ form_end(form) }}
    

</div>
{% endblock %}

